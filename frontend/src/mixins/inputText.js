export default {
  props: {
    maxlength: {
      type: Number,
      default: 0
    },
    minlength: {
      type: Number,
      default: 0
    },
    autofocus: {
      type: Boolean,
      default: false
    }
  }
}
